# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Ngô Kỳ Anh
- Mã học viên: 2A202602916
- Nhóm: ACC
- Repository/branch: https://github.com/mnic2904/K4-L3A-RAG-Pipeline-ACC/tree/KyAnh

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 2: Crawl Data | Viết script crawl 5 bài viết du lịch (không chọn nguồn) | `src/task2_crawl_news.py` | Done |
| Task 3: Convert MD | Dùng thư viện `MarkItDown` chuẩn hoá dữ liệu sang Markdown | `src/task3_convert_markdown.py` | Done |
| Task 4: Chunk & Index | Chunk văn bản, gọi API Embedding và upsert ChromaDB | `src/task4_chunking_indexing.py` | Done |
| Task 5: Semantic Search | Xây dựng hàm query ChromaDB trả về kết quả dense search | `src/task5_semantic_search.py` | Done |
| Task 6: Lexical Search | Xây dựng index BM25 và hàm tính lexical score | `src/task6_lexical_search.py` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:**Tham gia chọn nguồn PDF phù hợp với code hiện có
   **Lý do/evidence:** Có những nguồn PDF ko phù hợp với code hiện có nên đã chọn nguồn PDF phù hợp
   **Trade-off:** Dành thời gian hoàn thiện các task khác cho việc này

2. **Quyết định:**  Đề xuất sử dụng OPENAI embedding thay cho SENTENCE-TRANSFORMER vì thời gian tải xuống là quá lâu
   **Lý do/evidence:** OPENAI embedding cho kết quả tốt hơn trong RAG
   **Trade-off:** Phải sử dụng API KEY có tốn phí

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
    """
    Đây là các lệnh tôi đã dùng để test pipeline:
    python -m src.task2_crawl_news
    python -m src.task3_convert_markdown
    python -m src.task4_chunking_indexing
    python -m src.task5_semantic_search
    python -m src.task6_lexical_search
    """
- Kết quả trước/sau nếu có:
    """
    Danh sách các thư mục:
    - /chroma_db/: ChromaDB local (task 4, 5, 6)
    - /data/standardized/: Markdown đã chuẩn hóa (task 3)
    - /data/landing/news/: File json chứa 5 bài viết du lịch do nhóm crawl được (task 2)
    """
- Lỗi đã phát hiện và cách xử lý: Thư viện chromium bị xung khắc nên đã tiến hành dùng thư viện MarkItDown 

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Phần task 2 chưa được tối ưu hoá về nguồn dữ liệu có thể crawl, chưa hỗ trợ nhiều định dạng file
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: tối ưu hoá phần task 2 để có thể crawl nhiều nguồn dữ liệu khác nhau và hỗ trợ nhiều định dạng file

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/9/2026
- Tên thành viên: Ngô Kỳ Anh
